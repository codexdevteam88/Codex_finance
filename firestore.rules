rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down by default; relax for development as needed.
    match /{document=**} {
      allow read: if request.time < timestamp.date(2099, 1, 1);
      allow write: if false; // writes via backend import/functions only
    }
  }
}